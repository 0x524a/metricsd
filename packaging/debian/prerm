#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        # Stop service if running
        if systemctl is-active metricsd.service >/dev/null 2>&1; then
            systemctl stop metricsd.service || true
        fi
        ;;
esac

exit 0
